$(document).ready(function () {
    console.log('This site was generated by Hugo.');

    var currentPath = window.location.pathname; // Gets the relative path of the current URL
    console.log(currentPath);

//  if (currentPath.indexOf("your-substring") > -1) { // Check if the path contains the substring
//    $('your-element-selector').addClass('active'); // Add the "active" class to your element
//  }

});
